{
  "name": "crashpad",
  "version": "0.1.0",
  "description": "A local Apple crash report parser and root-cause analysis harness.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/twidtwid/crashpad.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/server.js",
    "test": "node --test test/*.test.mjs",
    "analyze": "node scripts/analyze-folder.js",
    "check": "node --check src/app.js && node --check src/stats.js && node --check src/crashParser.js && node --check src/i18n/en.js && node --check src/i18n/dom.js && node --check src/i18n/page.js && node --check scripts/server.js && node --check scripts/analyze-folder.js",
    "ci": "npm run check && npm test"
  }
}
